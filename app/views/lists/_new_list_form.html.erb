<%= simple_form_for list,
    data: {
      insert_list_target: "form",
      action: "submit->insert-list#send"
    } do |f| %>
  <%= f.input :name %>
  <%= f.input :photo, as: :file %>
  <%= f.submit "Add list" , "class": "btn btn-primary w-100 mt-3" %>
<% end %>
