<%= simple_form_for path,
    data: {
      insert_bookmark_target: "form",
      action: "submit->insert-bookmark#send"
    } do |f| %>
  <%= f.association :movie %>
  <%= f.input :comment %>
  <%= f.submit "Add bookmark", class: "btn btn-primary w-100 mt-3" %>
<% end %>
